apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: gamebuddy
  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: gamebuddy
spec:
  selector:
    app.kubernetes.io/name: frontend
  ports:
    - name: http
      port: 80
      targetPort: http
